import ConfirmModal from 'views/modal/ConfirmModal';
import InfoModal from 'views/modal/InfoModal';

const MODAL_COMPONENTS = {
  CONFIRM_MODAL: { key: 'CONFIRM_MODAL', modal: ConfirmModal },
  INFO_MODAL: { key: 'INFO_MODAL', modal: InfoModal },
};

export default MODAL_COMPONENTS;
