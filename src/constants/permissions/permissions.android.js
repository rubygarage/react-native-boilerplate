import { PERMISSIONS } from 'react-native-permissions';

export const { CAMERA } = PERMISSIONS.ANDROID;
export const MICROPHONE = PERMISSIONS.ANDROID.RECORD_AUDIO;
export const { WRITE_EXTERNAL_STORAGE } = PERMISSIONS.ANDROID;
