import { PERMISSIONS } from 'react-native-permissions';

import { UNSUPPORTED } from './common';

export const { CAMERA } = PERMISSIONS.IOS;
export const { MICROPHONE } = PERMISSIONS.IOS;
export const WRITE_EXTERNAL_STORAGE = UNSUPPORTED;
